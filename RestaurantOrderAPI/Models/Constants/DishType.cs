namespace RestaurantOrderAPI.Models.Constants
{
    public enum DishType
    {
        Entrée,
        Side,
        Drink,
        Dessert,

        //Error must be the last one
        Error
    }
}
